{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch main.go in cmd/api",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/api",
            "cwd": "${workspaceFolder}",
            "env": {},
            "args": []
        }
    ]
}